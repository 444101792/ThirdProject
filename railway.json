{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd SaudiEstate && python manage.py migrate && python manage.py loaddata users.json && python manage.py loaddata properties.json && python manage.py loaddata inquiries.json && python manage.py collectstatic --noinput && gunicorn SaudiEstate.wsgi"
    }
}
